---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: bookinfo
spec:
  hosts:
  - "bookshop.local"
  - "bookshop.me"
  gateways:
  - istio-ingress/istio-default-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: nginx-service.default.svc.cluster.local
        port:
          number: 80
---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  hosts:
  - "longhorn-ui.local"
  gateways:
  - istio-ingress/istio-default-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: longhorn-frontend
        port:
          number: 80
# ---
# apiVersion: networking.istio.io/v1alpha3
# kind: VirtualService
# metadata:
#   name: postgres
# spec:
#   hosts:
#   - "bookshop.me"
#   gateways:
#   - istio-ingress/istio-default-gateway
#   http:
#   - match:
#     - uri:
#         prefix: /
#     route:
#     - destination:
#         host: postgres-service.default
#         port:
#           number: 5432
---
apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: external-minio
  namespace: default
spec:
  hosts:
  - minio.local
  addresses:
  - 192.168.3.13/32
  ports:
  - number: 9000
    name: minio-api
    protocol: HTTP
  - number: 9001
    name: minio-console
    protocol: HTTP
  resolution: STATIC
  endpoints:
  - address: 192.168.3.13
    ports:
      minio-api: 9000
      minio-console: 9001
  location: MESH_EXTERNAL 
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: external-minio
  namespace: default
spec:
  hosts:
  - "minio.local"
  gateways:
  - istio-ingress/istio-default-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: minio.local
        port:
          number: 9001
    timeout: 30s
---
apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: minio-api
  namespace: default
spec:
  hosts:
  - minio-api.local
  addresses:
  - 192.168.3.13/32
  ports:
  - number: 9000
    name: http
    protocol: HTTP
  resolution: STATIC
  location: MESH_EXTERNAL
  endpoints:
  - address: 192.168.3.13
    ports:
      http: 9000
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: minio-api
  namespace: default
spec:
  hosts:
  - "minio-api.local"
  gateways:
  - istio-ingress/istio-default-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: minio-api.local
        port:
          number: 9000
# ---
# apiVersion: networking.istio.io/v1alpha3
# kind: VirtualService
# metadata:
#   name: mlflow
#   namespace: mlflow
# spec:
#   hosts:
#     - "mlflow.local"
#   gateways:
#     - istio-ingress/istio-default-gateway
#   http:
#     - match:
#         - uri:
#             prefix: /
#       route:
#         - destination:
#             host: mlflow.mlflow.svc.cluster.local
#             port:
#               number: 80
# ---
# apiVersion: networking.istio.io/v1alpha3
# kind: VirtualService
# metadata:
#   name: triton
#   namespace: triton
# spec:
#   hosts:
#     - "triton.local"
#   gateways:
#     - istio-ingress/istio-default-gateway
#   http:
#     - match:
#         - uri:
#             prefix: /
#       route:
#         - destination:
#             host: tritonio-triton-inference-server.triton.svc.cluster.local
#             port:
#               number: 8000